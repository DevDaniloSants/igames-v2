/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "i.ibb.co",
      },
      {
        protocol: "https",
        hostname: "blog-igames.s3.amazonaws.com",
      },
    ],
  },
};

export default nextConfig;
